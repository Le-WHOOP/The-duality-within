using UnityEngine;

// The Alambic is in the laboratory and is used to make the antidote
public class Alambic : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
